import { StriveSingleSelectGroupDirective } from './single-select.directive';

describe('StriveSingleSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new StriveSingleSelectGroupDirective();
    expect(directive).toBeTruthy();
  });
});
